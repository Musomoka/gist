
<% if !user_signed_in? %>

<div class="jumbotron">
<h1>"The Science of Brevity"</h1>
<p>"inspid irreleant unkown inlikable rough tedious coluorful bored congruent insufficient</p>
<p><% link_to "Sign up to write A review", new_user_registration_path, class:"btn btn-primarty btn-lg"%></p>
</div>
<%end%>

<div class="row">
  <%@movies.each do |movie|%>
  
  <div class=".col-sm-6 col-md-3">
    <div class="thumbnail">
      <%=link_to (image_tag movie.image.url(:medium), class:'image'),movie %>
    </div>
 </div>

  <%end%>
</div>